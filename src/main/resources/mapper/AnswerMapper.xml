<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.message.management.mapper.AnswerMapper">

	<select id="getByQueryCode"> SELECT * FROM answer WHERE query_code = #{queryCode} </select>

	<select id="getAllByUserId"> SELECT * FROM answer WHERE user_id = #{userId} </select>


</mapper>